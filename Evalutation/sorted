import pandas as pd
import glob

df_final = pd.read_csv('finalResult.csv', sep=";", index_col=0)
df_final.index = df_final.index.astype(int)  # Sicherstellen, dass der Index als Integer behandelt wird
df_final = df_final.sort_index()
df_final.to_csv("newdf.csv", sep=';')